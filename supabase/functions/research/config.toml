# Configuration for research edge function
# Make sure to set PERPLEXITY_API_KEY in your Supabase project secrets

[function]
verify_jwt = false  # Allow unauthenticated calls for simplicity (consider securing in production)
